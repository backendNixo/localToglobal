// import express from "express";
// import expressProxy from "express-http-proxy";



// const PORT = process.env.PORT||7000;
// const app = express();

// app.use("/user", expressProxy("http://localhost:7001"));
// app.use("/email", expressProxy("http://localhost:7002"));
// app.use("/tunnel", expressProxy("http://localhost:7003"));
// app.use("/plan", expressProxy("http://localhost:7004"));
// app.use("/payment", expressProxy("http://localhost:7005"));

// app.listen(PORT, () => {
//   console.log(`API Gateway running on port ${PORT}`);
// });
